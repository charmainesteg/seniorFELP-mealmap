import { Outlet } from 'react-router-dom';


function PantryLayout() {
  return (
    <main>
      <section>Main Pantry Layout Page</section>
      <Outlet />
    </main>
  );
}

export default PantryLayout;
